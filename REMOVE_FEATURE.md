# MessCal calendar controls

Added:
- Remove MessCal events button
- Confirmation dialog
- Only deletes events tagged by MessCal
- Disconnect Google Calendar button
- Google OAuth revoke attempt

For a multi-user production deployment, move OAuth tokens and event ownership into a database instead of process memory.
